araba modelleri

await page.goto("https://www.arabam.com/ikinci-el/sahibinden?searchText=mercedes&sort=priceTl.asc");
    const names  =await page.$$eval("div.listing-text-new.word-break.val-middle.color-black2018",(texts)=>{
        return texts.map((x) => x.textContent);

      
    });
    console.log(names);
    await browser.close();

    araba fiyatları

     await page.goto("https://www.arabam.com/ikinci-el?searchText=e+200&sort=priceTl.asc");
    const names  =await page.$$eval("span.db.no-wrap.listing-price",(texts)=>{
        return texts.map((x) => x.textContent);

      
    });

    console.log(names);